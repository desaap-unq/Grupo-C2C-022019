language: java
dist: trusty
sudo: required
jdk:
 - oraclejdk8
install:
 - cd viandasYa-backend && mvn clean install -Dmaven.compiler.target=1.8 -Dmaven.compiler.source=1.8

 # comment install if it does not work